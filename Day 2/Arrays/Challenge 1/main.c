#include <stdio.h>

int main(){
      int integers[]={1,2,3,4,5};

      printf("%d",integers[0]);
            printf("\n");
      printf("%d",integers[1]);
            printf("\n");
      printf("%d",integers[2]);
            printf("\n");
      printf("%d",integers[3]);
            printf("\n");
      printf("%d",integers[4]);
            printf("\n");

    return 0;
}
/*
Challenge 1: Initialization and Display
Write a C program that initializes an array of integers with given values ​​and displays those values. 
For example, you can initialize an array with the values [1, 2, 3, 4, 5]​​and display each value on a new line.
*/